# Infra (Phase 3 — not built yet)

Infrastructure-as-code for the AWS backend (Lambda, DynamoDB, EventBridge
Scheduler, IAM least-privilege role, budget alarm). Tooling (CDK vs SAM vs
Terraform) to be decided when we build Phase 3 — leaning AWS CDK in TypeScript so
it shares a language with the rest of the project.
